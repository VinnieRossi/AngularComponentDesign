export interface ApplicationSettings {
    indentAmount: number; // Number of spaces to indent properties, eg. 2
    applicationStyling?: string; // Type of styling within application, eg. "css", "sass"
    applicationPrefix: string; // Prefix for components, eg. 'app'
}